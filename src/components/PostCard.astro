---
interface Props {
  title: string
  url: string
  excerpt?: string
  pubDate: Date | number
  categories?: string[]
  tags?: string[]
}
const { title, url, excerpt = '', pubDate, categories = [], tags = [] } = Astro.props
const date = typeof pubDate === 'number' ? new Date(pubDate) : pubDate
---
<article class="section-card p-5 rounded-2xl shadow-sm hover:shadow-lg transition-all duration-200 border-l-4" style="border-left-color:var(--card-accent)">
  <h2 class="font-semibold mb-2" style="font-size:var(--text-lg)">
    <a href={url} class="transition-opacity hover:opacity-80" style="color:var(--text)">{title}</a>
  </h2>
  {excerpt && <p class="mb-3 line-clamp-2" style="color:var(--text-muted);font-size:var(--text-sm)">{excerpt}</p>}
  <p class="mb-3" style="color:var(--text-muted);font-size:var(--text-xs)">{date.toLocaleDateString('zh-CN')}</p>
  <div class="flex flex-wrap gap-2">
    {categories.map((c) => (
      <span class="px-2.5 py-1 rounded-lg" style="background:var(--card-border);color:var(--text);font-size:var(--text-xs)">{c}</span>
    ))}
    {tags.map((t) => (
      <span class="px-2.5 py-1 rounded-lg" style="color:var(--accent-2);font-size:var(--text-xs)">{t}</span>
    ))}
  </div>
</article>
