---
description: Astro 页面与布局约定 — 简洁信息卡片风格
globs: '**/*.astro'
alwaysApply: false
---

# Astro 页面约定

## 布局

- 前台页面用 `BlogLayout.astro`，传入 `title` prop
- 后台页面用 `AdminLayout.astro`，路由以 `/admin` 开头
- 主内容区：`max-w-6xl mx-auto w-full px-4 sm:px-6 py-6 md:py-8`
- 主内容容器 `id="pjax-main"`

## 数据获取

```astro
const posts = await getCollection('post', ({ data }) => data.published)
const [setting] = await getCollection('setting')
const siteName = setting?.data.siteSetting?.siteName ?? '博客'
```

## 页面视觉与文案

所有页面遵循「简洁信息卡片」语言：

- 优先呈现真实信息（标题、摘要、分类、标签、时间），避免终端拟态符号和命令词（如 `$`、`#`、`export`、`cd`、`cat`、`ls`、`[INFO]`）
- 避免重复信息（如 slug 行重复标题、路径行重复分组标题）与低信息量占位文案（如“页面详情”“已准备就绪”）
- 颜色 **全部** 通过 CSS 变量，禁止硬编码色值

## 语义化样式类（优先复用）

| 类名             | 用途                                              |
| ---------------- | ------------------------------------------------- |
| `section-card`   | 卡片容器（白/暗底、边框、圆角、hover 高亮）       |
| `section-title`  | 区块标题，使用直观中文标题                         |
| `code-label`     | 辅助信息小字（mono、`--text-xs`、`--text-muted`） |
| `terminal-list`  | 列表容器（历史命名保留，按普通列表使用）           |
| `post-card-cute` | 文章卡片                                          |
| `animate-in`     | 入场动画，配合 `animate-in-delay-1` ~ `10`        |

## 导入框架组件

在 `.astro` 中导入 Vue/React 组件时，使用与文件名 **不同** 的别名以避免 Astro 编译器局部声明冲突：

```astro
---
import CommentComp from '../components/vue/Comment.vue'
---
<CommentComp client:load postId="/about" />
```
